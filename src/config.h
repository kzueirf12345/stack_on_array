#ifndef SRC_CONFIG_H
#define SRC_CONFIG_H


#define HASH_PROTECT
#define PENGUIN_PROTECT

#define TEST_MODE


#define MAX(a, b) (((a) > (b)) ? (a) : (b))
#define MIN(a, b) (((a) < (b)) ? (a) : (b))


#endif /*SRC_CONFIG_H*/